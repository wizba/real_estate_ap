using System;

namespace Constants.SourceType {
     // Enum to represent the type of the source (Person, Property, etc.)
    public enum SourceType
    {
        Person = 1,
        Property = 2
    }
}